# projects/livehome

这个目录当前保留为项目覆盖层样例目录，用于承接仍然依赖具体业务目录、流程或例外口径的规则。

凡是仍然直接依赖以下内容的，都优先归到这里：

- 某个项目自己的业务目录边界
- 该项目独有的任务板、派发索引和运行态约束
- 只对单一项目成立的接口规范、权限规则、初始化要求和入口闭环约束
- 仅对特定研发流程成立的 Prompt、Agent 约束或守卫要求

后续继续通用化时，默认先把强项目绑定规则留在覆盖层，再逐步识别哪些部分可以继续上移到 core/ 或 stacks/。
